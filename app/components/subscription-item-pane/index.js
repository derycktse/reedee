import { connect } from 'react-redux'
import SubscriptionItemView from './subscription-item-view'

function mapStateToProps(state) {
  return {

  }
}

export default connect(mapStateToProps)(SubscriptionItemView)
